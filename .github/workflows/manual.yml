on:
  workflow_dispatch:
    inputs:
      enviroment:
        description: "choose env"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy application
        run: echo "deploying app"
        
